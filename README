= Sow Seeds

These are my personl Sow templates. They can be installed via:

  $ sow --install git://github.com/trans/seeds.git

Then any directory in the repository can be used as scaffold.

  $ sow seed myproj/

You can setup up you own pack of seeds just as easily.
See http://proutils.github.com/sow.

